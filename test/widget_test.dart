import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches successfully', (WidgetTester tester) async {
    // TODO: Firebase初期化が必要なため、後でテストを実装
    expect(true, true);
  });
}
